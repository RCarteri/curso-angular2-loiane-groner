var minhaVar  = 'minha variavel';

function minhaFunction(x, y) { return x + y; }

let num = 2;
const PI = 3.14;
var numeros = [1, 2, 3];
numeros.map(function (n) { return n * 2;})

numeros.map(n => { n * 2;})

class Matematica {
    soma(x, y) { return x + y; }
}